import { ConcluidaDirective } from './concluida.directive';

describe('ConcluidaDirective', () => {
  it('should create an instance', () => {
    const directive = new ConcluidaDirective();
    expect(directive).toBeTruthy();
  });
});
